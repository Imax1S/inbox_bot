# Telegram Bot (get token from @BotFather)
TELEGRAM_BOT_TOKEN=your_bot_token
TELEGRAM_USER_ID=your_telegram_user_id

# LLM Configuration
# Provider: "claude" or "openai"
LLM_PROVIDER=claude
# Optional: specify custom model (otherwise uses default for provider)
# LLM_MODEL=gpt-4o

# API Keys (use the one matching your LLM_PROVIDER)
ANTHROPIC_API_KEY=your_anthropic_api_key
OPENAI_API_KEY=your_openai_api_key

# Obsidian Vault Path (inside container, mapped via docker-compose)
OBSIDIAN_VAULT_PATH=/vault/life/weekly
